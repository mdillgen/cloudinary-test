# Codemod-driven ESLint flat config migration

ESLint will be migrated from `next lint` to direct `eslint` CLI invocation with flat config (`eslint.config.mjs`), generated by the `@next/codemod upgrade` codemod rather than hand-written. The repo has no custom ESLint rules to preserve, so the codemod output is sufficient.

**Considered options**: Manual flat config setup; defer linting to a follow-up PR.
